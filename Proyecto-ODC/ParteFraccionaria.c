#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "Validacion.h"

char * cambiarBaseParteFraccionaria (char * numero, short * baseSource, short * baseDestination) {
    short * check;
    long * resultado;

    check = verificarNumero(numero, baseSource);
}
